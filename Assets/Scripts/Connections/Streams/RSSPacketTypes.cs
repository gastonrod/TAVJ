namespace Connections.Streams
{
    public enum RSSPacketTypes
    {
            SPAWNED_PLAYER = 0,
            ACK,
            INIT_CONNECTION,
            DESTROY_OBJECT,
            CREATE_OBJECT
    }
}